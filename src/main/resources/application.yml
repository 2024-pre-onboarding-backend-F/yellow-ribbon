spring:
  application:
  name: ribbon

  profiles:
    include:
      - secret

  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.spatial.dialect.mysql.MySQL8SpatialDialect # Hibernate 6.x.x에서 MySQL의 공간 함수를 사용하기 위한 설정

    hibernate:
      ddl-auto: update

  defer-datasource-initialization: true # ????? 2.5 ???? ?? sql ??? ??? ???

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver # cj? ?????

  sql: # ?? ??????? sql ????? ????
    init:
      mode: always

  data:
    redis:
      port: 6379
